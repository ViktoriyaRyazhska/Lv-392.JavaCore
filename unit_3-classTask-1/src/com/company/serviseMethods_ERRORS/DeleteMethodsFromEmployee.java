package com.company.serviseMethods_ERRORS;

public class DeleteMethodsFromEmployee {




    /**
     * this method calculate salary of month
     * @return
     */
/*
    public */
/*static*//*
 double calculateSalary() {
        double salary = positionRageOfHour.getRageOfHour() * getHourOfMonth();
        return salary;
    }
*/

    /**
     * this method calculate rate(bonus) to salary
     * @return
     */
/*
    public */
/*static*//*
 double changeRate(){
        double changeBonus = 0;
        if(getHourOfMonth() > 150) {
            changeBonus = calculateSalary() * 0.1;
//            newRate += changeBonus;
        } else if(getHourOfMonth() > 200) {
            changeBonus = calculateSalary() * 0.2;
//            newRate += changeBonus;
        } else{
//            newRate += changeBonus;
        }
//        return newRate;
        return changeBonus;
    }
*/

    /**
     * this method calculate salary  + rate(bonus)
     * @return
     */
/*
    public */
/*static*//*
 double sumSalary() {
        double sumSalary = calculateSalary() + changeRate();
        return  sumSalary;
    }
*/

}
